apiVersion: apps/v1
kind: Deployment
metadata:
  name: webbot
  namespace: bots
  labels:
    app: webbot
    type: ishbot
spec:
  template:
    metadata:
      name: webbot-pod
      labels:
        app: webbot
        type: ishbot
    spec:
      containers:
        - name: webbot
          image: us-east4-docker.pkg.dev/ish-bot/ish-bot/ish_bot:19c7e65d98cc7802a3de1dad0e8084425ea1a897
          envFrom:
            - secretRef:
                name: ish-bot-proxy-auths
          ports:
            - containerPort: 5900
              name: vnc
          resources:
          volumeMounts:
          - name: storage
            mountPath: /ebs
          - name: config-volume
            mountPath: /app/configs

      volumes:
      - name: storage
        persistentVolumeClaim:
          claimName: ish-bot-pvc
      - name: config-volume
        configMap:
          name: ish-bot-config

  replicas: 1
  selector:
    matchLabels:
      type: ishbot
