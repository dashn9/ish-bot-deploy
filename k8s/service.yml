apiVersion: v1
kind: Service
metadata:
  name: vnc-service
  namespace: bots
spec:
  type: NodePort
  selector:
    name: webbot
    type: bot
  ports:
  - protocol: TCP
    port: 5900
    targetPort: 5900
